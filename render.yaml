services:
  - type: web
    name: smart-leads-app
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET_KEY
        sync: false
